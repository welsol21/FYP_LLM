export function AnalyzeListPage() {
  return (
    <section className="card">
      <h2>Analyze Files (Stub)</h2>
      <p>
        Legacy cross-project analyze list from `temp/ela.zip`. Current flow: `Projects` {'->'} `Files` {'->'} double-tap
        file {'->'} `Analyze`.
      </p>
    </section>
  )
}
